- name: Restart Docker
  ansible.builtin.systemd:
    name: docker
    state: restarted

- name: Stop Docker
  ansible.builtin.systemd:
    name: docker
    state: stopped

- name: Force restart Docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
    daemon_reload: yes